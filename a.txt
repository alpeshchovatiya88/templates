exp_bins = pd.cut(
    exp,
    bins=bin_edges,
    include_lowest=True
)

act_bins = pd.cut(
    act,
    bins=bin_edges,
    include_lowest=True
)

# Convert bucket labels to strings
exp_bins = exp_bins.astype("string").fillna("MISSING")
act_bins = act_bins.astype("string").fillna("MISSING")