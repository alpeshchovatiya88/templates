df_rest = (
    df_all
    .merge(
        df_1[["APPLICATION_NUMBER"]].drop_duplicates(),
        on="APPLICATION_NUMBER",
        how="left",
        indicator=True
    )
    .query('_merge == "left_only"')
    .drop(columns="_merge")
    .reset_index(drop=True)
)

print(df_rest.shape)