collection_overlap = (
    collection.assign(
        Bankruptcy = (collection["CRA_BANKRUPTCY_FORECLOSURE_IND"]=="Y"),
        Judgement  = (collection["CRA_JUDGEMENT"]=="Y"),
        PublicDerog= (collection["CRD_PUBLIC_DEROG_TU"]>0)
    )
    .groupby(["Bankruptcy","Judgement","PublicDerog"])
    .size()
    .reset_index(name="Count")
    .sort_values("Count", ascending=False)
)

collection_overlap
