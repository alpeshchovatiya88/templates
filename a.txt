summary_table = pd.DataFrame({
    "YEAR": years,
    "DELQ_COUNT": delq_count.values,
    "CLAIM_COUNT": claim_count.values,
    "DELQ_HPI_RATIO": delq_ratio.values,
    "CLAIM_HPI_RATIO": claim_ratio.values
})

summary_table["DELQ_HPI_CHANGE_%"] = (
    (summary_table["DELQ_HPI_RATIO"] - 1) * 100
)

summary_table["CLAIM_HPI_CHANGE_%"] = (
    (summary_table["CLAIM_HPI_RATIO"] - 1) * 100
)

print(summary_table.round(3))
