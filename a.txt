pct = pd.crosstab(
    df["BOOKYEAR"],
    df["PRD_LOAN_PURPOSE"],
    normalize="index"
).mul(100)

count = pd.crosstab(
    df["BOOKYEAR"],
    df["PRD_LOAN_PURPOSE"]
)

table = pct.copy()

for col in table.columns:
    table[col] = (
        pct[col].map("{:.2f}%".format)
        + " ("
        + count[col].map("{:,}".format)
        + ")"
    )

display(table)